# === Secrets / environment ===
# .env holds local secrets and must NOT be committed. Only .env.example is tracked.
.env
*.pem
*.key
appsettings.*.local.json

# === .NET (api/) ===
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
*.user
*.suo
project.lock.json
TestResults/
[Tt]est[Rr]esults/
*.trx
.dotnet/

# === Elixir / Phoenix (ui/) ===
_build/
deps/
*.ez
erl_crash.dump
.elixir_ls/
.fetch
# Compiled assets produced by esbuild/tailwind + phx.digest
ui/priv/static/assets/
ui/priv/static/cache_manifest.json

# === Node / asset tooling ===
node_modules/
npm-debug.log*

# === Docker ===
**/pgdata/
docker-compose.override.yml

# === IDE / editor ===
.vs/
.vscode/
.idea/
*.swp
*.swo

# === OS ===
.DS_Store
Thumbs.db
desktop.ini

# === MS Office temp / lock files ===
~$*
*.tmp

# === Logs / temp / scratch ===
*.log
logs/
tmp/
.__write_test__
