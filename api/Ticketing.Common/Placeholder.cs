namespace Ticketing.Common;

// Phase 0 placeholder so the project compiles.
// Shared types, result type (ErrorOr<T> approach), and enums are added in later phases.
internal static class Placeholder
{
}
